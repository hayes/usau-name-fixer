{
  "name": "usau-urban-dictionary-fix",
  "version": "1.0.0",
  "type": "module",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && pnpm build:web",
    "build:web": "rm -rf public/extension public/extension.zip && mkdir -p public && cp index.html public/ && cp -r dist public/extension && cd public/extension && zip -r ../extension.zip . && cd ../..",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "@types/node": "^20.12.7",
    "typescript": "^5.4.5",
    "vite": "^5.2.10",
    "vite-plugin-web-extension": "^4.1.6"
  }
}
